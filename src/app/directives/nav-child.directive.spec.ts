import { NavChildDirective } from './nav-child.directive';

describe('NavChildDirective', () => {
  it('should create an instance', () => {
    const directive = new NavChildDirective();
    expect(directive).toBeTruthy();
  });
});
